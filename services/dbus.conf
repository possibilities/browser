[program:dbus]
command=/bin/bash -lc 'mkdir -p /run/dbus && dbus-uuidgen --ensure && dbus-daemon --config-file=/etc/dbus-1/container-system.conf --nopidfile --nosyslog --nofork'
autostart=false
autorestart=true
startsecs=2
stdout_logfile=/var/log/supervisord/dbus
redirect_stderr=true
